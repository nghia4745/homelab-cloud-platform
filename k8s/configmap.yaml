apiVersion: v1
kind: ConfigMap
metadata:
  name: homelab-api-config
  namespace: homelab
data:
  # Environment name consumed by the application.
  APP_ENV: "dev"
  # Log verbosity consumed by the application.
  LOG_LEVEL: "info"
